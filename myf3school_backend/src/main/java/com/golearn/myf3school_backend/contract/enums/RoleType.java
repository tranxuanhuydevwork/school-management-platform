package com.golearn.myf3school_backend.contract.enums;

public enum RoleType {
    ADMIN,
    TEACHER,
    STUDENT,
    PARENT,
    CLUB_ADVISOR,   // Teacher assigned as a club supervisor
    STAFF           // Non-teaching staff (accountant, admin clerk, etc.)
}